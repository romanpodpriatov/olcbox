<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>ProofKit</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!--
		Export compliance, answered once here instead of on every upload.

		True, and it is the accurate answer rather than the convenient one. The
		exemptions App Store Connect offers cover an app whose encryption is the
		operating system's, or is HTTPS, or protects only credentials or DRM.
		This app is none of those: it carries traffic inside Reality, Hysteria2
		with Salamander obfuscation, and olcRTC's own encrypted transport, and it
		decrypts its own subscription links. Answering "no" to save a form would
		be a false declaration.

		What it obliges — and this part is the company's, not the code's — is a
		one-time BIS Encryption Registration Number and the annual
		self-classification report for 5D992.c mass-market software. Apple asks
		for that documentation once, not per release. `ITSEncryptionExportCompliance
		Code` belongs here too, but only if a CCATS is ever obtained; there is
		none, so the key is absent rather than empty.

		The algorithms are standard — AES, ChaCha20-Poly1305, TLS — which is what
		the "proprietary algorithms" question in the questionnaire is asking
		about. Our protocols are bespoke; the ciphers underneath are not.
	-->
	<key>ITSAppUsesNonExemptEncryption</key>
	<true/>
	<key>NSCameraUsageDescription</key>
	<string>ProofKit uses the camera only to scan a subscription QR code. Nothing is recorded or sent anywhere.</string>
	<!--
		No UIBackgroundModes.

		`audio` was declared for the silent-audio keep-alive that kept the in-app
		SOCKS proxy alive in the background. olcRTC runs in the packet tunnel
		extension now, `SwiftOlcRtcManager.start` is unreachable from Kotlin, and
		that keep-alive never runs — so the app declared a background mode it does
		not use, which App Store review rejects on sight (2.5.4). The tunnel needs
		no background mode of its own: a NEPacketTunnelProvider is its own process
		and the system keeps it running.
	-->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
